# Админ панель - Currency API

## Доступ

Админ панель доступна по адресу:
```
http://localhost:8000/admin/
```

**Пароль по умолчанию:** `admin123`

⚠️ **ВАЖНО:** В продакшене обязательно измените пароль!

## Возможности

### 📊 Дашборд

Главная страница показывает:

1. **Статистика за сегодня:**
   - Общее количество запросов
   - Успешные запросы
   - Ошибки
   - Среднее время ответа
   - Уникальные устройства
   - Всего устройств

2. **Популярные endpoints:**
   - Список самых используемых endpoints
   - Количество запросов
   - Среднее время ответа

3. **Вкладки:**
   - **Конвертации** - Последние конвертации валют и популярные пары
   - **Ошибки** - Последние ошибки системы
   - **Обновления курсов** - История обновлений курсов валют

### 📋 Логи запросов

Полный список всех запросов к API с возможностью фильтрации:

- По endpoint
- По HTTP статусу
- По устройству

**Доступ:** `http://localhost:8000/admin/logs.php`

**Отображаемые данные:**
- Время запроса
- HTTP метод
- Endpoint
- Статус ответа
- Время ответа (мс)
- Размер ответа (байты)
- Устройство
- IP адрес
- Параметры запроса

## Автоматическое обновление

Дашборд автоматически обновляется каждые 30 секунд.

## Безопасность

Текущая реализация использует простую проверку пароля через сессию. Для продакшена рекомендуется:

1. Использовать хеширование паролей (bcrypt)
2. Добавить защиту от брутфорса
3. Использовать HTTPS
4. Добавить двухфакторную аутентификацию
5. Ограничить доступ по IP (опционально)

## Изменение пароля

Чтобы изменить пароль, отредактируйте файл `admin/index.php`:

```php
if ($_POST['password'] === 'admin123') {  // Замените 'admin123' на ваш пароль
```

Или лучше используйте переменную окружения или конфигурационный файл.

## Структура данных

Админ панель использует следующие таблицы:

- `api_requests` - Логи всех запросов
- `currency_conversions` - Конвертации валют
- `error_logs` - Ошибки системы
- `rate_update_logs` - Обновления курсов
- `devices` - Зарегистрированные устройства
- `api_statistics` - Агрегированная статистика

## Требования

Для работы админ панели необходимо:

1. Запустить миграцию расширенного логирования:
   ```bash
   php database/migrations_extended_logging.php
   ```

2. Убедиться, что таблицы созданы и заполняются данными

3. PHP сессии должны работать (по умолчанию включены)

