# Настройка Git репозитория

## ✅ Текущий статус

Репозиторий инициализирован и первый коммит создан.

**Коммит:** `ffb8ebc` - Initial commit

## 📤 Загрузка в GitHub/GitLab

### Вариант 1: Создать новый репозиторий на GitHub

1. Создайте новый репозиторий на GitHub (без README, .gitignore и лицензии)

2. Добавьте remote и выполните push:

```bash
# Добавить remote (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Или через SSH
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Загрузить код
git push -u origin main
```

### Вариант 2: Существующий репозиторий

```bash
# Добавить remote
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>

# Загрузить код
git push -u origin main
```

## 📋 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить изменения
git push

# Получить изменения
git pull
```

## 🔒 Что исключено из репозитория (.gitignore)

- `/vendor/` - зависимости Composer
- `*.log` - логи
- `.env` - переменные окружения
- `.DS_Store` - системные файлы macOS
- IDE файлы (`.idea/`, `.vscode/`)

## ⚠️ Важно перед публикацией

1. **Проверьте конфигурацию:**
   - `config/database.php` - не содержит реальных паролей
   - `admin/index.php` - пароль по умолчанию `admin123`

2. **Рекомендуется:**
   - Использовать переменные окружения для паролей
   - Добавить `.env.example` файл
   - Не коммитить реальные данные БД

3. **Создайте `.env.example`:**
```env
DB_HOST=localhost
DB_NAME=currency_api
DB_USER=root
DB_PASS=
ADMIN_PASSWORD=admin123
```

## 📝 Структура проекта в Git

```
currency_api/
├── .gitignore
├── README.md
├── API_DOCUMENTATION_COMPLETE.md
├── composer.json
├── config/
├── api/
├── admin/
├── models/
├── services/
├── database/
└── scripts/
```

