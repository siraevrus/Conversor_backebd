# 📊 Резюме: Рекомендации по логированию данных

## Что уже логируется

✅ Базовая информация о запросах:
- ID устройства
- Endpoint
- HTTP метод
- IP адрес
- User Agent
- Время запроса

## 🎯 Рекомендуемые дополнительные данные

### 1. **Производительность** ⚡
- ✅ Время выполнения запроса (мс)
- ✅ Использование памяти (КБ)
- ✅ Размер ответа (байты)

**Зачем:** Мониторинг производительности, выявление узких мест

---

### 2. **Отладка** 🐛
- ✅ HTTP статус код ответа
- ✅ Параметры запроса (JSON, ограничение 5000 символов)
- ✅ Сообщения об ошибках

**Зачем:** Быстрое воспроизведение проблем, диагностика

---

### 3. **Аналитика** 📈
- ✅ Референр (откуда пришел запрос)
- ✅ Версия API
- ✅ Логирование конвертаций валют (отдельная таблица)
- ✅ Агрегированная статистика по дням

**Зачем:** Понимание использования API, бизнес-метрики

---

### 4. **Мониторинг обновлений курсов** 🔄
- ✅ Успешность обновления
- ✅ Время выполнения обновления
- ✅ Время ответа внешнего API
- ✅ Количество обновленных курсов
- ✅ Источник обновления (cron/manual/api)

**Зачем:** Отслеживание стабильности обновлений курсов

---

### 5. **Централизованное логирование ошибок** ❌
- ✅ Тип ошибки
- ✅ Сообщение об ошибке
- ✅ Трассировка стека
- ✅ Параметры запроса при ошибке
- ✅ HTTP статус

**Зачем:** Централизованное отслеживание всех ошибок

---

## 📋 Новые таблицы

1. **`currency_conversions`** - Логирование всех конвертаций
2. **`rate_update_logs`** - Логирование обновлений курсов
3. **`error_logs`** - Централизованное логирование ошибок
4. **`api_statistics`** - Агрегированная статистика по дням

## 🔧 Расширенные поля в `api_requests`

- `response_status` - HTTP статус код
- `response_time_ms` - Время выполнения
- `request_params` - Параметры запроса
- `response_size_bytes` - Размер ответа
- `referer` - Референр
- `api_version` - Версия API
- `error_message` - Сообщение об ошибке
- `memory_usage_kb` - Использование памяти

## 🚀 Как внедрить

1. Запустите миграцию:
   ```bash
   php database/migrations_extended_logging.php
   ```

2. Используйте класс `Logger` в коде:
   ```php
   $logger = new Logger();
   $logger->logRequest(...);
   $logger->logConversion(...);
   $logger->logError(...);
   ```

3. Обновите API роутер для использования Logger

## 💡 Примеры использования данных

### Популярные конвертации:
```sql
SELECT from_currency, to_currency, COUNT(*) 
FROM currency_conversions 
GROUP BY from_currency, to_currency
ORDER BY COUNT(*) DESC;
```

### Медленные endpoints:
```sql
SELECT endpoint, AVG(response_time_ms) 
FROM api_requests 
GROUP BY endpoint 
ORDER BY AVG(response_time_ms) DESC;
```

### Статистика ошибок:
```sql
SELECT error_type, COUNT(*) 
FROM error_logs 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 24 HOUR)
GROUP BY error_type;
```

## 📚 Подробная документация

См. файл `LOGGING_RECOMMENDATIONS.md` для детального описания всех рекомендаций.

