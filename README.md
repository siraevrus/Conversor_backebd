# Currency API

API для получения актуальных курсов валют и конвертации валют для мобильного приложения.

## 📚 Документация

- **[📖 Полная документация API](API_DOCUMENTATION_COMPLETE.md)** - Полное описание всех endpoints с примерами (рекомендуется)
- **[⚡ Краткая справка](API_QUICK_REFERENCE.md)** - Быстрый справочник по основным endpoints
- **[📝 Детальная документация](API_DOCUMENTATION.md)** - Подробное описание всех endpoints
- **[💡 Примеры использования](api_examples.md)** - Примеры кода на разных языках
- **[🌐 Интерактивная документация](http://localhost:8000/api-docs.html)** - Swagger UI (откройте после запуска сервера)
- **[📋 OpenAPI спецификация](swagger.yaml)** - Swagger/OpenAPI 3.0 спецификация

## Возможности

- Автоматическое обновление курсов валют каждые 60 минут
- REST API для мобильного приложения
- Регистрация и отслеживание устройств
- Конвертация валют
- История запросов API
- **Админ панель** для мониторинга и аналитики

## Требования

- PHP >= 7.4
- MySQL >= 5.7
- Расширения PHP: PDO, curl, json
- Веб-сервер (Apache/Nginx)

## Установка

### 1. Клонирование и настройка

```bash
cd currency_api
```

### 2. Настройка базы данных

Отредактируйте файл `config/database.php` и укажите параметры подключения к MySQL:

```php
private $host = "localhost";
private $db_name = "currency_api";
private $username = "root";
private $password = "ваш_пароль";
```

### 3. Создание базы данных

```sql
CREATE DATABASE currency_api CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. Запуск миграций

```bash
php database/migrations.php
```

Или через composer:

```bash
composer migrate
```

### 5. Настройка веб-сервера

#### Apache

Убедитесь, что модуль `mod_rewrite` включен. Файл `.htaccess` уже настроен.

#### Nginx

Добавьте в конфигурацию:

```nginx
location / {
    try_files $uri $uri/ /api/index.php?$query_string;
}

location ~ \.php$ {
    fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
    fastcgi_index index.php;
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
}
```

## Настройка автоматического обновления курсов

### Cron (рекомендуется)

Добавьте в crontab:

```bash
crontab -e
```

Добавьте строку:

```
0 * * * * /usr/bin/php /полный/путь/к/проекту/currency_api/scripts/update_rates.php >> /var/log/currency_api.log 2>&1
```

Это будет обновлять курсы каждый час.

### Альтернативный способ

Можно использовать планировщик задач вашего хостинга или вызывать endpoint `/api/update` по расписанию.

## API Endpoints

### Базовый URL

```
http://ваш-домен/api
```

### 1. Информация об API

**GET** `/api/`

Возвращает список доступных endpoints.

### 2. Получение курсов валют

**GET** `/api/rates`

Получить все курсы валют (базовая валюта USD по умолчанию).

**GET** `/api/rates?base=USD&target=EUR`

Получить курс конкретной валюты.

**Параметры:**
- `base` (опционально) - базовая валюта (по умолчанию USD)
- `target` (опционально) - целевая валюта

**Пример ответа:**

```json
{
    "success": true,
    "base": "USD",
    "target": "EUR",
    "rate": 0.85,
    "last_updated": "2024-01-15 12:00:00"
}
```

### 3. Конвертация валют

**GET** `/api/convert?amount=100&from=USD&to=EUR`

Конвертировать сумму из одной валюты в другую.

**Параметры:**
- `amount` (обязательно) - сумма для конвертации
- `from` (обязательно) - исходная валюта
- `to` (обязательно) - целевая валюта
- `base` (опционально) - базовая валюта для расчета (по умолчанию USD)

**Пример ответа:**

```json
{
    "success": true,
    "amount": 100,
    "from": "USD",
    "to": "EUR",
    "converted_amount": 85.00,
    "rate": 0.85,
    "last_updated": "2024-01-15 12:00:00"
}
```

### 4. Регистрация устройства

**POST** `/api/device/register`

Зарегистрировать или обновить информацию об устройстве.

**Тело запроса (JSON):**

```json
{
    "device_id": "unique-device-id-12345",
    "device_name": "iPhone 13",
    "device_type": "mobile",
    "platform": "iOS",
    "app_version": "1.0.0"
}
```

**Пример ответа:**

```json
{
    "success": true,
    "message": "Устройство зарегистрировано",
    "device_id": 1
}
```

### 5. Информация об устройстве

**GET** `/api/device/info?device_id=unique-device-id-12345`

Получить информацию об устройстве.

**Параметры:**
- `device_id` (обязательно) - уникальный идентификатор устройства

### 6. Принудительное обновление курсов

**POST** `/api/update`

Принудительно обновить курсы валют (не дожидаясь следующего cron).

## Структура базы данных

### Таблица `exchange_rates`

Хранит курсы валют.

- `id` - первичный ключ
- `base_currency` - базовая валюта (например, USD)
- `target_currency` - целевая валюта
- `rate` - курс обмена
- `last_updated` - время последнего обновления
- `created_at` - время создания записи

### Таблица `devices`

Хранит информацию об устройствах.

- `id` - первичный ключ
- `device_id` - уникальный идентификатор устройства
- `device_name` - название устройства
- `device_type` - тип устройства
- `platform` - платформа (iOS, Android и т.д.)
- `app_version` - версия приложения
- `last_active` - время последней активности
- `created_at` - время регистрации
- `updated_at` - время последнего обновления

### Таблица `api_requests`

Хранит историю запросов к API.

- `id` - первичный ключ
- `device_id` - ссылка на устройство
- `endpoint` - endpoint API
- `method` - HTTP метод
- `ip_address` - IP адрес
- `user_agent` - User Agent
- `created_at` - время запроса

## Примеры использования

### cURL

```bash
# Получить все курсы
curl http://localhost/api/rates

# Конвертировать валюту
curl "http://localhost/api/convert?amount=100&from=USD&to=EUR"

# Зарегистрировать устройство
curl -X POST http://localhost/api/device/register \
  -H "Content-Type: application/json" \
  -d '{
    "device_id": "test-device-123",
    "device_name": "Test Device",
    "platform": "iOS"
  }'
```

### PHP

```php
$url = 'http://localhost/api/convert?amount=100&from=USD&to=EUR';
$response = file_get_contents($url);
$data = json_decode($response, true);
echo $data['converted_amount'];
```

## Безопасность

- В продакшене отключите отображение ошибок PHP
- Используйте HTTPS
- Добавьте аутентификацию для административных endpoints
- Ограничьте частоту запросов (rate limiting)
- Валидируйте все входные данные

## Логирование

Скрипт обновления курсов логирует результаты в stdout/stderr. При настройке cron рекомендуется перенаправлять вывод в файл:

```bash
0 * * * * /usr/bin/php /path/to/update_rates.php >> /var/log/currency_api.log 2>&1
```

## Админ панель

Проект включает админ панель для мониторинга и аналитики:

**Доступ:** `http://localhost:8000/admin/`  
**Пароль по умолчанию:** `admin123`

### Возможности админ панели:

- 📊 **Дашборд** - Статистика за сегодня, популярные endpoints
- 💱 **Конвертации** - История конвертаций и популярные пары валют
- ❌ **Ошибки** - Централизованное отслеживание ошибок
- 🔄 **Обновления курсов** - История обновлений курсов валют
- 📋 **Логи запросов** - Полный список всех запросов с фильтрацией

Подробнее см. [admin/README.md](admin/README.md)

## Лицензия

MIT

