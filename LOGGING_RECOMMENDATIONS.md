# Рекомендации по логированию данных

## 📊 Текущее логирование

Сейчас логируется базовая информация о запросах в таблице `api_requests`:
- `device_id` - ID устройства
- `endpoint` - Endpoint API
- `method` - HTTP метод
- `ip_address` - IP адрес
- `user_agent` - User Agent браузера/приложения
- `created_at` - Время запроса

## 🚀 Рекомендуемые дополнительные данные для логирования

### 1. Производительность и мониторинг

#### Время выполнения запроса (`response_time_ms`)
- **Зачем:** Отслеживание производительности API
- **Использование:** Выявление медленных endpoints, оптимизация
- **Формат:** Миллисекунды (INT)

#### Использование памяти (`memory_usage_kb`)
- **Зачем:** Мониторинг потребления ресурсов
- **Использование:** Выявление утечек памяти, оптимизация
- **Формат:** Килобайты (INT)

#### Размер ответа (`response_size_bytes`)
- **Зачем:** Анализ трафика и оптимизация
- **Использование:** Сжатие ответов, кэширование
- **Формат:** Байты (INT)

### 2. Отладка и диагностика

#### Параметры запроса (`request_params`)
- **Зачем:** Воспроизведение проблемных запросов
- **Использование:** Отладка ошибок, анализ использования API
- **Формат:** JSON (TEXT, ограничение 5000 символов)

#### HTTP статус код (`response_status`)
- **Зачем:** Отслеживание успешных/неуспешных запросов
- **Использование:** Мониторинг ошибок, статистика
- **Формат:** INT (200, 400, 404, 500 и т.д.)

#### Сообщения об ошибках (`error_message`)
- **Зачем:** Детальная информация об ошибках
- **Использование:** Быстрое выявление проблем
- **Формат:** TEXT

### 3. Аналитика и бизнес-метрики

#### Референр (`referer`)
- **Зачем:** Отслеживание источников трафика
- **Использование:** Аналитика, маркетинг
- **Формат:** VARCHAR(500)

#### Версия API (`api_version`)
- **Зачем:** Отслеживание использования разных версий API
- **Использование:** Планирование депрекации старых версий
- **Формат:** VARCHAR(10)

### 4. Специализированное логирование

#### Логирование конвертаций валют (`currency_conversions`)
**Отдельная таблица для аналитики:**
- `amount` - Исходная сумма
- `from_currency` - Исходная валюта
- `to_currency` - Целевая валюта
- `converted_amount` - Конвертированная сумма
- `rate` - Использованный курс
- `device_id` - Устройство
- `ip_address` - IP адрес
- `created_at` - Время конвертации

**Зачем:**
- Анализ популярных пар валют
- Статистика использования конвертера
- Выявление паттернов использования

#### Логирование обновлений курсов (`rate_update_logs`)
**Отдельная таблица для мониторинга:**
- `base_currency` - Базовая валюта
- `rates_count` - Количество обновленных курсов
- `update_source` - Источник обновления (cron, manual, api)
- `success` - Успешность обновления
- `error_message` - Сообщение об ошибке (если есть)
- `execution_time_ms` - Время выполнения обновления
- `api_response_time_ms` - Время ответа внешнего API
- `created_at` - Время обновления

**Зачем:**
- Мониторинг стабильности обновлений
- Выявление проблем с внешним API
- Анализ производительности

#### Логирование ошибок (`error_logs`)
**Отдельная таблица для ошибок:**
- `device_id` - Устройство
- `endpoint` - Endpoint где произошла ошибка
- `error_type` - Тип ошибки (Exception, PDOException, etc.)
- `error_message` - Сообщение об ошибке
- `stack_trace` - Трассировка стека
- `request_params` - Параметры запроса
- `ip_address` - IP адрес
- `user_agent` - User Agent
- `http_status` - HTTP статус код
- `created_at` - Время ошибки

**Зачем:**
- Централизованное отслеживание ошибок
- Быстрое выявление проблем
- Анализ типичных ошибок

### 5. Агрегированная статистика (`api_statistics`)

**Ежедневная статистика по endpoints:**
- `date` - Дата
- `endpoint` - Endpoint
- `method` - HTTP метод
- `total_requests` - Всего запросов
- `successful_requests` - Успешных запросов
- `failed_requests` - Неуспешных запросов
- `avg_response_time_ms` - Среднее время ответа
- `total_response_size_bytes` - Общий размер ответов
- `unique_devices` - Уникальных устройств

**Зачем:**
- Быстрая аналитика без запросов к большим таблицам
- Дашборды и отчеты
- Мониторинг трендов

## 📈 Преимущества расширенного логирования

### Для разработки:
1. **Быстрая отладка** - Все данные запроса доступны в логах
2. **Производительность** - Выявление узких мест
3. **Мониторинг** - Отслеживание здоровья API

### Для бизнеса:
1. **Аналитика** - Понимание использования API
2. **Оптимизация** - Данные для принятия решений
3. **Безопасность** - Выявление подозрительной активности

### Для поддержки:
1. **Воспроизведение** - Легко воспроизвести проблему
2. **Статистика** - Ответы на вопросы пользователей
3. **Отчеты** - Автоматическая генерация отчетов

## 🔒 Безопасность и приватность

### Рекомендации:
1. **Не логировать чувствительные данные:**
   - Пароли, токены, API ключи
   - Персональные данные (если не требуется)

2. **Ограничение размера:**
   - Параметры запроса ограничены 5000 символов
   - Референр ограничен 500 символами

3. **Ротация логов:**
   - Старые логи можно архивировать
   - Удалять логи старше определенного периода

4. **Доступ:**
   - Логи должны быть доступны только администраторам
   - Использовать индексы для быстрого поиска

## 📝 Примеры использования

### Анализ популярных конвертаций:
```sql
SELECT from_currency, to_currency, COUNT(*) as count
FROM currency_conversions
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY from_currency, to_currency
ORDER BY count DESC
LIMIT 10;
```

### Мониторинг производительности:
```sql
SELECT endpoint, AVG(response_time_ms) as avg_time, 
       MAX(response_time_ms) as max_time,
       COUNT(*) as requests
FROM api_requests
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 1 HOUR)
GROUP BY endpoint
ORDER BY avg_time DESC;
```

### Отслеживание ошибок:
```sql
SELECT error_type, COUNT(*) as count, 
       MAX(created_at) as last_occurrence
FROM error_logs
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 24 HOUR)
GROUP BY error_type
ORDER BY count DESC;
```

## 🚀 Внедрение

1. Запустите миграцию расширенного логирования:
   ```bash
   php database/migrations_extended_logging.php
   ```

2. Обновите код для использования нового Logger класса

3. Настройте автоматическую очистку старых логов (опционально)

4. Создайте дашборды для визуализации данных (опционально)

