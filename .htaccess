RewriteEngine On

# Разрешаем доступ к админ панели
RewriteCond %{REQUEST_URI} ^/admin/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^admin/(.*)$ admin/index.php [QSA,L]

# Перенаправление всех запросов на api/index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^api/(.*)$ api/index.php [QSA,L]

# Разрешаем доступ к статическим файлам (html, css, js, images)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^.*$ - [L]

# Перенаправление корневого запроса на api (только если файл не существует)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^$ api/index.php [L]

